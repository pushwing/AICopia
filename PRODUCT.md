# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**주 사용자 — 쇼핑객(스토어프론트).** 미래 디자인 작업의 1차 대상은 상품을 탐색·비교하고 장바구니에 담아 결제하는 최종 소비자다. 모바일·데스크톱 웹 브라우저에서 방문하며, 구매 결정을 내리는 흐름(홈 → 목록/검색 → 상세 → 장바구니 → 주문 → 결제)이 경험의 중심이다.

부차 사용자(디자인 우선순위는 낮지만 실재하는 사용자):

- **쇼핑몰 운영자(관리자).** 납품받은 가맹점 담당자가 `/admin`에서 상품·재고·주문·회원·쿠폰·포인트를 운영하고 AI 보조 기능(카테고리 추천, 리뷰 요약, 문의 자동분류, 발주 제안, 이상주문 탐지 등)을 사용한다. 화면 성공 기준은 스캔 용이성·일관성·업무 완료 속도.
- **납품하는 웹에이전시(개발자/셋업).** 보일러플레이트를 빠르게 커스터마이징·납품한다. 코어는 재사용하고 테마(껍데기)만 교체하는 것이 핵심 작업.

## Product Purpose

AICopia는 **CodeIgniter 4 기반의 납품용 쇼핑몰 보일러플레이트**다. 기업형 홈페이지(동적 페이지·다중 게시판·문의)와 풀 쇼핑몰(상품·옵션/SKU·장바구니·주문·PG 결제·포인트·쿠폰·회원 등급)을, AI 운영 보조 레이어 위에 하나의 CI4 프로젝트로 함께 제공한다.

존재 이유는 **1인(소규모) 웹에이전시의 빠른 납품**이다. 성공은 "코어 코드는 손대지 않고 테마·설정 교체만으로 새 클라이언트에게 중형 쇼핑몰을 짧은 시간에 납품"하는 것으로 정의된다.

## Positioning

이웃 제품이 그대로 베끼기 어려운 지점:

- **코어 재사용 + 테마 교체 화이트라벨 구조.** 하나의 CI4 코어를 유지한 채 `app/Views/themes/{name}/`·`public/themes/{name}/` 오버라이드와 ZIP 업로드 설치·원클릭 전환으로 납품처별 껍데기만 바꾼다(`ThemeView` 렌더러).
- **AI 운영 보조 레이어가 코어에 내장.** 상품 설명/카테고리 생성, 이미지→상품정보(Vision), 리뷰 요약·부정리뷰 감지, 문의 자동분류·답변 초안, 매출 자연어 리포트, 재입고 제안·이상주문 탐지(휴리스틱)까지 운영자 업무를 줄이는 기능을 기본 제공. 제공자(Groq·Claude·OpenRouter)를 설정에서 선택하고, 프롬프트를 코드 수정 없이 편집하며, API 키 미설정 시 자동 폴백/비활성화된다.
- **엔터프라이즈 사이트 + 쇼핑몰 통합.** 동적 페이지·GNB 메뉴·다중 게시판·배너/팝업·문의 등 기업형 CMS 기능과 풀 커머스를 한 프로젝트로 납품.

## Operating Context

- **스토어프론트 구매 흐름:** 홈/Welcome(Hero·카테고리·기획전·신상품·할인 섹션) → 상품 목록(카테고리·가격/할인 필터·키워드 검색·정렬) → 상품 상세(다중 이미지·옵션 SKU·재고·QnA·리뷰·찜) → 빠른 장바구니(모달) → 장바구니(게스트/회원 공용, 쿠폰·포인트 적용) → 주문(카카오 주소 API·배송 요청) → 결제(5개 PG + 무통장입금).
- **마이페이지:** 주문 내역·배송 추적·구매 확정, 취소/반품/교환 신청, 간편 재주문, 배송지 관리, 쿠폰/포인트 내역, 찜 목록.
- **회원 인증:** 이메일 가입(24시간 내 인증 활성화, 재발송 1분 쿨다운) + 소셜 로그인(Google·Naver·Kakao OAuth2). 회원 등급은 구매 실적 기반 자동 산정, 등급별 포인트 적립률 차등.
- **관리자 패널(`/admin`):** 대시보드 통계, 상품/카테고리/재고/주문/배송/쿠폰/포인트/회원등급/회원/매입처/기획전/리뷰/QnA/게시판/배너·팝업/페이지/메뉴/미디어/문의/사이트설정/테마 관리. 목록형 화면 다수가 AG Grid 기반.
- **배치/스케줄:** 주문 만료, 등급 재계산, 생일 쿠폰, 접속로그 정리, AI 잡 워커(`ai_jobs`)가 크론(`tasks:run`)으로 동작.
- **주문 상태 흐름은 단방향 강제:** `pending → paid → preparing → shipped → delivered`(+무통장 `awaiting_payment`, 미결제 30분 후 `expired`, 취소/반품/교환/환불 분기). 반품/교환 기간은 `delivered_at` 기준 7일.

## Capabilities and Constraints

- **스택은 기존 코드베이스가 확정:** CodeIgniter 4 / PHP 8.5+, 단일 저장소 = 단일 CI4 프로젝트(하위 프로젝트 없음). 데이터 접근은 Query Builder, 뷰는 네이티브 대체 문법 + `esc()`.
- **테마 탐색 순서:** `themes/{active_theme}/{view}` → `themes/default/{view}` → `app/Views/{view}`(관리자·콘텐츠 뷰는 테마화 대상 아님). 새 테마는 `default`와 다른 부분만 오버라이드.
- **전역 데이터 주입:** 모든 컨트롤러가 `BaseController` 상속, `$settings`·`$menus`·`$authUser`·`$cartCount` 등을 매 요청 주입. 뷰는 컨트롤러가 전달한 데이터만 렌더링(뷰에서 Model 직접 조회 금지).
- **재고 차감은 결제 성공(또는 무통장 확인) 시점에만.** 장바구니 담기 시점 차감 금지. 동시성 2단계 가드(`SELECT ... FOR UPDATE` + 조건부 UPDATE) + `payments.pg_tid` UNIQUE로 중복 콜백 방어.
- **PG 어댑터:** `bank_transfer`·`toss`·`inicis`·`nicepay`·`kakaopay`·`naverpay`·`payco`(`PGFactory`/`PGInterface`).
- **타임존 규약:** DB 저장 UTC / 앱·화면 KST(Asia/Seoul). 시각 컬럼은 `TIMESTAMP`(MySQL이 세션 `+09:00`으로 자동 변환), 순수 날짜는 `DATE`. ⚠️ `TIMESTAMP` 상한 2038-01-19.
- **AI 미결/폴백:** API 키 미설정 시 각 AI 기능은 자동 비활성화 또는 일반 동작으로 폴백(추천·발주·이상탐지·검색 일부는 LLM 없는 휴리스틱).

## Brand Commitments

**브랜드 모델 = 제품명 + 화이트라벨 병행.** 보일러플레이트 제품 자체의 이름은 **AICopia**(에이전시·개발자 맥락에서 노출)이지만, **납품된 스토어프론트는 클라이언트 브랜드로 교체**된다 — 테마·로고·색·카피가 납품처마다 달라진다.

- 스토어프론트 디자인 작업은 **특정 고정 브랜드 정체성에 묶이지 않는다.** 기본 테마(`default`)는 어떤 클라이언트 브랜드로도 교체 가능한 중립적·범용적 기반이어야 하며, 브랜드 요소(로고·색·서체)는 테마/설정 레이어에서 주입된다.
- 사이트명·연락처·SNS·SEO·푸터·로고 등은 하드코딩이 아니라 `settings`(관리자 사이트 설정)에서 온다 — 디자인은 이 값들이 런타임에 주입된다는 전제로 만든다.
- 설치용 예시 테마: `default`, `dark`, `spring`(violet), `themes/` 아래 ZIP 배포본 존재.

## Evidence on Hand

- **현재 성격은 데모/포트폴리오용.** 실제 상용 운영 레퍼런스가 아니라 에이전시 영업·포트폴리오를 위한 데모로 운용된다. 스토어프론트 데이터는 시더(`ProductSeeder`·`PostSeeder` 등) 기반 샘플이다.
- **미래 작업이 지어내면 안 되는 것:** 실제 판매 실적, 실 거래 고객·후기, 매출 벤치마크, 특정 상용 납품처의 실명·사례. 이런 사회적 증거는 현재 존재하지 않으므로 화면에 사실처럼 넣지 않는다. 리뷰·후기·통계가 필요하면 명백히 샘플/데모임을 전제로 다룬다.
- **자료:** `README.md`(기능 전체 목록), `docs/manual.md`(고객+관리자 통합 매뉴얼), `AICopia_사용매뉴얼.pdf`(워크스페이스 루트), `docs/roadmap.md`, `docs/seo-geo-strategy.md`.

## Product Principles

1. **테마는 껍데기, 코어는 불변.** 스토어프론트 표현(색·서체·레이아웃 뉘앙스)은 테마 레이어에서 바뀌고, 코어 뷰·로직은 납품처가 달라도 재사용된다. 디자인은 기본 테마를 "어느 브랜드로도 갈아입힐 수 있는 중립 기반"으로 만든다.
2. **구매 전환이 스토어프론트의 성공 척도.** 탐색→상세→장바구니→결제의 마찰을 줄이고 스캔·비교·결정을 쉽게 하는 것이 최우선. 표현은 이 흐름을 방해하지 않는 선에서.
3. **설정·데이터는 런타임 주입.** 사이트명·로고·연락처·메뉴·배너·상품은 모두 `settings`/DB에서 온다. 화면은 특정 값을 하드코딩하지 않고, 값이 비었을 때(빈 상태)도 무너지지 않아야 한다.
4. **AI는 운영자를 돕는 보조 레이어이지 필수 의존이 아니다.** 키 미설정·폴백 상태에서도 제품이 정상 동작해야 한다. AI 산출물은 초안·제안으로 제시하고 최종 판단은 사람에게.
5. **증거는 사실만.** 데모/포트폴리오 단계이므로 실적·후기·고객 사례를 지어내지 않는다. 사회적 증거가 필요하면 샘플임을 전제한다.
